/**
 * BullMQ queue definitions (docs §6.7.1). No queue is created yet — add one
 * here, plus a matching worker under jobs/workers/, when the first module
 * that needs deferred work (billing -> PDF/WhatsApp) is implemented.
 */
export {}
